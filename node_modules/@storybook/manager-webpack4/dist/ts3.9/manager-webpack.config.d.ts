import { Configuration } from 'webpack';
import { Options, ManagerWebpackOptions } from '@storybook/core-common';
declare const _default: ({ configDir, configType, docsMode, entries, refs, outputDir, previewUrl, versionCheck, releaseNotesData, presets, modern, }: Options & ManagerWebpackOptions) => Promise<Configuration>;
export default _default;
